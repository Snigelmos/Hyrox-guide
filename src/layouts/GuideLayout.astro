---
import BaseLayout from "./BaseLayout.astro";
import ShareButtons from "../components/ShareButtons.astro";
import GuideCrossSell from "../components/GuideCrossSell.astro";
import StickyTOC from "../components/StickyTOC.astro";
import { formatDate } from "../data/blog";
import { findAuthorByName } from "../data/authors";

interface HowToStep {
  name: string;
  text: string;
  image?: string;
}

interface Props {
  title: string;
  description?: string;
  breadcrumbs?: { label: string; href?: string }[];
  showIllustration?: boolean;
  crossSellContext?: "training" | "racing" | "general";
  schemaType?: "WebSite" | "Article" | "FAQPage" | "CollectionPage";
  datePublished?: string;
  dateModified?: string;
  author?: string;
  ogImage?: string;
  faqItems?: { question: string; answer: string }[];
  howToSteps?: HowToStep[];
  howToName?: string;
  showMeta?: boolean;
  /** When true, render a sticky in-article TOC sidebar (long posts). */
  showToc?: boolean;
  /** When true, emit `<meta robots="noindex, follow">` (scheduled / preview pages). */
  noindex?: boolean;
}

const {
  title,
  description,
  breadcrumbs = [],
  showIllustration = false,
  crossSellContext = "general",
  schemaType = "Article",
  datePublished,
  dateModified,
  author,
  ogImage,
  faqItems,
  howToSteps,
  howToName,
  showMeta = false,
  showToc = false,
  noindex = false,
} = Astro.props;

const schemaBreadcrumbs = breadcrumbs.map((crumb) => ({
  name: crumb.label,
  ...(crumb.href ? { url: crumb.href } : {}),
}));

const publishedDate = datePublished ? new Date(datePublished) : undefined;
const modifiedDate = dateModified ? new Date(dateModified) : undefined;
const authorProfile = findAuthorByName(author);

const sidebarLinks = [
  { href: "/racing-guide/", icon: "🏁", label: "Racing Guide" },
  { href: "/training/", icon: "💪", label: "Training Programs" },
  { href: "/gyms/map/", icon: "📍", label: "Find a Hyrox Gym" },
  { href: "/supplements/", icon: "💊", label: "Supplements" },
  { href: "/calculator/", icon: "⏱️", label: "Time Calculator" },
  { href: "/competitions/", icon: "📅", label: "Competitions" },
  { href: "/faq/", icon: "❓", label: "FAQ" },
];
---

<BaseLayout
  title={`${title} | Hyrox Vault`}
  description={description}
  ogType="article"
  schemaType={schemaType}
  datePublished={datePublished}
  dateModified={dateModified}
  author={author}
  ogImage={ogImage}
  breadcrumbs={schemaBreadcrumbs}
  faqItems={faqItems}
  howToSteps={howToSteps}
  howToName={howToName ?? title}
  howToDescription={description}
  noindex={noindex}
>
  <div class="container-main py-6 md:py-16">
    {
      breadcrumbs.length > 0 && (
        <nav class="mb-4 md:mb-8 text-sm" aria-label="Breadcrumb">
          <ol class="flex items-center gap-2 flex-wrap">
            <li>
              <a
                href="/"
                class="text-text-muted hover:text-accent transition-colors no-underline"
              >
                Home
              </a>
            </li>
            {breadcrumbs.map((crumb, i) => (
              <li class="flex items-center gap-2">
                <span class="text-text-muted/40">/</span>
                {crumb.href && i < breadcrumbs.length - 1 ? (
                  <a
                    href={crumb.href}
                    class="text-text-muted hover:text-accent transition-colors no-underline"
                  >
                    {crumb.label}
                  </a>
                ) : (
                  <span class="text-text">{crumb.label}</span>
                )}
              </li>
            ))}
          </ol>
        </nav>
      )
    }

    {/* Mobile guide nav — visible below lg */}
    <details class="lg:hidden mb-4 bg-bg-card border border-border rounded-xl group">
      <summary class="flex items-center justify-between px-5 py-3 cursor-pointer min-h-[44px] text-sm font-semibold text-text-heading select-none">
        <span class="flex items-center gap-2">
          <svg class="w-4 h-4 text-accent" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h7" />
          </svg>
          Explore Guides
        </span>
        <svg class="w-4 h-4 text-text-muted transition-transform group-open:rotate-180" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
        </svg>
      </summary>
      <div class="px-5 pb-4 pt-1 border-t border-border/50">
        <ul class="space-y-1">
          {sidebarLinks.map((link) => (
            <li>
              <a
                href={link.href}
                class="flex items-center gap-2 text-sm text-text-muted hover:text-accent transition-colors no-underline py-2 min-h-[44px]"
              >
                <span class="text-base">{link.icon}</span> {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </details>

    <div class="lg:grid lg:grid-cols-[1fr_280px] lg:gap-16">
      <article class="min-w-0">
        <header class="mb-10 pb-8 border-b border-border relative overflow-hidden">
          {showIllustration && (
            <div class="absolute -right-6 -top-6 opacity-[0.06] pointer-events-none hidden md:block z-0">
              <slot name="illustration" />
            </div>
          )}
          <div class="relative z-10">
            <h1
              class="text-3xl md:text-5xl font-black leading-tight text-gradient-accent"
            >
              {title}
            </h1>
            {
              description && (
                <p class="mt-4 text-lg text-text-muted leading-relaxed max-w-2xl">
                  {description}
                </p>
              )
            }
            {
              showMeta && (publishedDate || modifiedDate || author) && (
                <div class="mt-5 flex flex-wrap items-center gap-x-4 gap-y-1 text-sm text-text-muted">
                  {author && (
                    authorProfile ? (
                      <span>
                        By{" "}
                        <a
                          href={`/author/${authorProfile.slug}/`}
                          class="text-text-heading font-semibold hover:text-accent transition-colors no-underline"
                          rel="author"
                        >
                          {author}
                        </a>
                      </span>
                    ) : (
                      <span>By <span class="text-text-heading font-semibold">{author}</span></span>
                    )
                  )}
                  {publishedDate && (
                    <time datetime={publishedDate.toISOString()}>
                      Published {formatDate(publishedDate)}
                    </time>
                  )}
                  {modifiedDate && (
                    <time datetime={modifiedDate.toISOString()} class="text-accent/80">
                      Updated {formatDate(modifiedDate)}
                    </time>
                  )}
                </div>
              )
            }
          </div>
        </header>

        {showToc && <StickyTOC variant="mobile" />}

        <div class="prose md:prose-lg max-w-none">
          <slot />
        </div>

        <ShareButtons title={title} />
        <GuideCrossSell context={crossSellContext} />
      </article>

      <aside class="hidden lg:block">
        <div class="sticky top-24 space-y-6">
          {showToc && <StickyTOC />}
          <div class="bg-bg-card border border-border rounded-2xl p-6">
            <h3 class="text-sm font-bold uppercase tracking-wider text-accent mb-4">
              Explore
            </h3>
            <ul class="space-y-3 text-sm">
              {sidebarLinks.map((link) => (
                <li>
                  <a
                    href={link.href}
                    class="flex items-center gap-2 text-text-muted hover:text-accent transition-colors no-underline"
                  >
                    <span class="text-base">{link.icon}</span> {link.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>

          <div class="bg-accent/5 border border-accent/10 rounded-2xl p-6">
            <h3 class="text-sm font-bold text-accent mb-2">
              Ready to race?
            </h3>
            <p class="text-xs text-text-muted mb-4 leading-relaxed">
              Find upcoming Hyrox events and register before they sell out.
            </p>
            <a
              href="/competitions/"
              class="inline-flex items-center gap-1 px-5 py-2.5 bg-accent text-bg text-xs font-bold rounded-lg hover:bg-accent-dim transition-colors no-underline min-h-[44px]"
            >
              Find a Race
              <svg
                class="w-3 h-3"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M9 5l7 7-7 7"></path>
              </svg>
            </a>
          </div>
        </div>
      </aside>
    </div>
  </div>

  {/* Mobile-only floating CTA. Dismissible per session and only appears
      after the reader has scrolled past the article header so it never
      covers content above the fold. */}
  <div
    id="mobile-floating-cta"
    class="lg:hidden fixed right-4 z-30 hidden items-center gap-1 bg-accent text-bg rounded-full shadow-lg shadow-accent/30 pl-4 pr-1.5 py-1.5"
    style="bottom: max(1rem, env(safe-area-inset-bottom));"
  >
    <a
      href="/calculator/"
      class="inline-flex items-center gap-2 text-xs font-bold text-bg no-underline py-1"
    >
      Try the calculator
      <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
      </svg>
    </a>
    <button
      type="button"
      id="mobile-floating-cta-close"
      class="ml-1 w-7 h-7 flex items-center justify-center rounded-full bg-bg/20 hover:bg-bg/40 text-bg cursor-pointer"
      aria-label="Dismiss"
    >
      <svg class="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
  </div>

  <script>
    // Floating "Try the calculator" CTA on guide pages. We only show it on
    // narrow viewports, after the user has scrolled at least 600px (so it
    // never covers the article header), and we respect a per-session dismiss.
    const cta = document.getElementById("mobile-floating-cta");
    const closeBtn = document.getElementById("mobile-floating-cta-close");
    if (cta) {
      const dismissed = sessionStorage.getItem("guide-cta-dismissed") === "1";
      const onCalculator = window.location.pathname.startsWith("/calculator");
      if (!dismissed && !onCalculator) {
        const onScroll = () => {
          if (window.scrollY > 600) {
            cta.classList.remove("hidden");
            cta.classList.add("inline-flex");
            window.removeEventListener("scroll", onScroll);
          }
        };
        window.addEventListener("scroll", onScroll, { passive: true });
        onScroll();
      }
      closeBtn?.addEventListener("click", () => {
        cta.classList.add("hidden");
        cta.classList.remove("inline-flex");
        sessionStorage.setItem("guide-cta-dismissed", "1");
      });
    }
  </script>
</BaseLayout>
